<?xml version="1.0" encoding="UTF-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:DWPennyFinder.ViewModels"
             x:DataType="viewmodels:ItemsViewModel"
             x:Class="DWPennyFinder.Views.ItemsView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Text="{Binding Id}" FontSize="Large"/>
        <Label Grid.Row="1" Text="{Binding Park}" FontSize="Medium"/>
        <Label Grid.Row="2" Text="{Binding Location}" FontSize="Medium"/>
        
        <Grid.GestureRecognizers>
            <TapGestureRecognizer Tapped="OnItemTapped"/>
        </Grid.GestureRecognizers>
    </Grid>
</ContentView>
